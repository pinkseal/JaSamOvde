# Заполняется автоматически Vercel при подключении базы (Storage → Postgres):
POSTGRES_URL=

# Придумайте сами: любая случайная строка от 32 символов.
# Можно сгенерировать командой: openssl rand -base64 32
SESSION_SECRET=
